import { redactSecrets } from '../utils/redaction.js';
import type { EnvConfig } from "../config/env.js"; import type { NormalizedIncomingMessage } from "./normalizeEvolutionMessage.js";  export interface SendTextInput {   message: NormalizedIncomingMessage;   text: string; }  export interface EvolutionSender {   sendText(input: SendTextInput): Promise<void>; }  export class EvolutionSendTextError extends Error {   constructor(     message: string,     public readonly httpStatus?: number   ) {     super(message);     this.name = "EvolutionSendTextError";   } }  export class EvolutionApiSender implements EvolutionSender {   constructor(private readonly env: EnvConfig) {}    async sendText(input: SendTextInput): Promise<void> {     const url = new URL(       `/message/sendText/${encodeURIComponent(this.env.evolutionInstance)}`,       this.env.evolutionApiBaseUrl     );     const number = input.message.chat_type === "group" ? input.message.remote_jid : input.message.phone_number;     const response = await fetch(url, {       method: "POST",       headers: {         "Content-Type": "application/json",         apikey: this.env.evolutionApiKey       },       body: JSON.stringify({         number,         text: input.text       })     });      if (!response.ok) {       const body = await response.text().catch(() => "");       throw new EvolutionSendTextError(         `Evolution sendText failed with ${response.status}: ${redactSecrets(body.slice(0, 300))}`,         response.status       );     }   } }
